const paramoteurs = document.querySelector("#paramoteur");
paramoteurs.addEventListener('click', (event) => {
    multiaxe.style.display = 'none'
    // console.log(event);
    // console.log(event.currentTarget);
});

const multi = document.querySelector(".ul multiaxe");

multi.addEventListener('click', (event) => {

});